#include <iostream>
#include <cctype>

using namespace std;

int main(int argc, char *argv[])
{
    /*
    char ch;
    unsigned m = 10;

    int i = 0;
    while (cin && m--) {
        cin.read(&ch, sizeof(ch));
        i++;
        if (ch == '\n')
        	cout << "OPA" << endl;
        cout << i << ": " << ch << endl;
    }
    return 0;

*/
    string s;
    while (cin >> s && s!= "DONE"){

        for ()
    }
    return 0;
}